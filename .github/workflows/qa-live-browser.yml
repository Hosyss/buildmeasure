name: QA live BuildNumbers browser smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-browser:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Browser QA checkpoint preserved; push trigger intentionally disabled during focused API diagnostics."
